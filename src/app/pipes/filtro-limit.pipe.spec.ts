import { FiltroLimitPipe } from './filtro-limit.pipe';

describe('FiltroLimitPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroLimitPipe();
    expect(pipe).toBeTruthy();
  });
});
